<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@drawable/bg_inviting_results"
    android:orientation="vertical">

    <com.project.mgjandroid.ui.view.newpulltorefresh.PullToRefreshListView
        android:id="@+id/lv_listView"
        android:layout_width="match_parent"
        android:layout_height="153dp"
        android:layout_alignParentBottom="true"
        android:cacheColorHint="#00000000"
        android:divider="#f95169"
        android:dividerHeight="13dp"
        android:fadingEdge="none"
        android:fastScrollEnabled="false"
        android:footerDividersEnabled="false"
        android:headerDividersEnabled="false"
        android:listSelector="@android:color/transparent"
        android:scrollbarThumbVertical="@drawable/xg_scrollbar"
        android:scrollbars="none" />

    <ImageView
        android:id="@+id/iv_dividing_line"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_above="@+id/lv_listView"
        android:layout_marginBottom="35dp"
        android:src="@drawable/ic_dividing_line" />

    <LinearLayout
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_centerHorizontal="true"
        android:layout_marginTop="@dimen/x150"
        android:gravity="center"
        android:orientation="vertical">

        <TextView
            android:id="@+id/tv_cash_back_sum"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_above="@+id/tv_query_result"
            android:layout_centerHorizontal="true"
            android:textColor="#e73a24"
            android:textSize="36sp" />

        <TextView
            android:id="@+id/tv_query_result"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="查看战果>>"
            android:textColor="#e73a24"
            android:textSize="13sp" />
    </LinearLayout>

</RelativeLayout>